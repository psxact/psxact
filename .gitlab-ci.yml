stages:
  - build

build_linux:
  stage: build
  image: debian:stretch
  script:
    - ./scripts/build.sh
    - ./scripts/package.sh
  artifacts:
    paths:
      - '*.tar.bz2'
    expire_in: 1 week
  tags:
    - docker
